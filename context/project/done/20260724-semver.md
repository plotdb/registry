# Task: @plotdb/registry semver range 支援

## 背景

makechart（及其它 servebase 專案）的 block 資源統一由 `/assets/lib/<name>/<version>/<path>`
提供，背後是 `@plotdb/registry`（repo: `~/workspace/plotdb/projects/registry`）的
`registry.route`：本地沒有的套件會依 provider chain（github → npm）自動抓取並 cache。

使用端範例：
 - makechart：`~/workspace/makechart/server/backend/makechart/registry.ls`
 - grantdash：`~/workspace/grantdash/v2/backend/grantdash`

目前 version 欄位只支援「精確版本」（如 `1.0.0`）或 `main`（最新）。
**沒有 semver range 的機制** — 不吃 `~1.0.0` / `^1.0.0` 這類語法。

## 需求

1. `@plotdb/registry` 支援 semver range 解析：
   - URL 中的 version segment 允許 range 語法（至少 `~x.y.z` 與 `^x.y.z`；
     URL encoding 需考慮，`^` 在 URL 中是 `%5E`，`~` 不需 encode）
   - resolve 規則：在 provider 可取得的版本清單中，取滿足 range 的**最新**版本
   - 預設策略（產品層期望）：圖表存檔時記精確版本，載入時 auto apply **minor/patch**
     更新（即以 `~x.y` / `^x.y.z` 語意解析；細節見下方開放問題）
2. cache 行為：
   - range resolve 結果需 cache，但要有失效機制（新 minor 版發佈後能被看到；
     可考慮 TTL 或 cachestamp 聯動）
   - 已 resolve 的精確版本內容 cache 不變（immutable）
3. 向後相容：
   - 精確版本與 `main` 的行為完全不變
   - 不合法的 range → 4xx（沿用 lderror 慣例）

## 決策（2026-07-24 與 tkirby 確認）

 - **range 解析層級**：registry.route 收到 range → resolve 出精確版本 → **302 redirect**
   至精確版本 URL。內容 URL 維持 immutable，range 目錄不落地磁碟
   （落地會被 nginx try_files 永遠直出、TTL 失效，見 `tasks/main-revalidate.md`）。
   range 的 TTL = nginx `proxy_cache_valid 302` 的 cache period + 版本清單 cache。
 - **版本清單來源**：github 用 `GET /repos/{owner}/{repo}/releases?per_page=100`
   （只取最近 100 筆、不翻頁；無 token 時 rate limit 60/hr，故清單必 cache）；
   npm 用 `registry.npmjs.org/<name>` metadata 的 `versions`。
   清單 cache 於 `<pkg>/.reg.versions.<provider>`，以 mtime + cachetime 判斷過期。
 - **main 語意不變**：維持「provider 最新 release」（含 major bump），不重定義。
   main 在 production nginx 下不會 revalidate 的問題另立 task（`tasks/main-revalidate.md`）。
 - **semver 實作**：沿用自有的 `@plotdb/semver`（瀏覽器輕量版，生態系共用）。
   已修 `fit` 的比較 bug 並新增 `compare` / `max-satisfying` / `valid` / `valid-range`
   （v0.0.4，registry 移入 dependencies）。
 - **db 策略**：registry 無 db；此題屬 makechart 應用層（傾向 db 存精確版、
   載入時轉 `^x.y.z`），不在本 task 範圍、不涉及 makechart 改動。

## 實作狀態

 - [x] `@plotdb/semver` v0.0.4：修 fit、加 compare / max-satisfying / valid / valid-range（需發佈）
 - [x] provider：`version-type` 加 range、`resolve`（走 provider chain + 清單 cache）、
   fetch 對 range 回 400（防止 range 目錄落地）
 - [x] providers：github / npm 加 `fetch-version-list`；修 npm provider name typo（原誤植 github）
 - [x] route：allowlist 放行 `~^%`、version segment decodeURIComponent、range → 302；
   不合法 version/range 回 400
 - [x] config.ngx：`proxy_cache_valid` 加 302 / 400
 - [x] `npm test`（`test/index.ls`）：stub provider 測 chain fallthrough、清單 cache 重用、
   force、prerelease 排除、302 redirect、400/404、range 目錄不落地；`npm run build` 建 dist
 - 已驗證：real npm/github chain resolve、%5E decode、302 target、cache 檔生成、
   400/404 行為、精確版本與 main 行為不變
 - codex review（`review/20260724/1.md`）已處理：移除 devDependencies 重複項、補 test/build
   scripts 與測試；`@plotdb/semver@0.0.4` 已發佈、lockfile 已更新
 - 2026-07-24 makechart + nginx 實測通過（makechart repo
   `context/project/tasks/registry-test-result.md`）：302 resolve、nginx cache HIT、
   range 不落地、回歸全過。附帶發現 session middleware 的 Set-Cookie 使 nginx
   從不 cache（既有問題），已在 config.ngx 範本加 `proxy_ignore_headers` +
   `proxy_hide_header`；**各部署（makechart / grantdash production）的 nginx config
   需重新生成套用**。staff flush + range 未實測（需 admin session，邏輯已有 unit test）

## 週邊脈絡（供參考，非本 task 範圍）

 - makechart 已將 block registry rule 統一為
   `frontend/web/src/pug/modules/block-registry.ls`（editor / viewer / sandbox / backend 共用），
   一律 canonical name（如 `@makechart/treemap`）+ version 預設 `main`
 - 舊的 `assets/block` 手動佈署目錄已更名 `assets/block-legacy` 待刪
 - db 中舊資料的 def 不做遷移，之後一律以 bid（ns, name, version, path）明確定義
